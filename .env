# Root environment file - loads auth keys from subdirectories
# This file is sourced when running 'docker compose' from the project root

# Production auth key (loaded from production/.env)
TS_AUTHKEY=${TS_AUTHKEY:-}

# Beta auth key (loaded from beta/.env)
TS_AUTHKEY_BETA=${TS_AUTHKEY_BETA:-}
