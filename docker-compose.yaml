# Root Docker Compose - Manages both Production and Beta stacks
#
# Usage:
#   docker compose up -d                      # Start both stacks
#   docker compose up -d --profile prod       # Start only production
#   docker compose up -d --profile beta       # Start only beta
#   docker compose down                       # Stop both stacks
#   docker compose ps                         # View all containers

services:
  # ============================================
  # PRODUCTION STACK
  # ============================================
  # Production - PINNED to stable version for reliability
  open-webui2:
    image: ghcr.io/open-webui/open-webui:v0.7.2-ollama
    container_name: open-webui2
    ports:
      - "11434:11434"

    profiles:
      - prod
      - all
    environment:
      BACKEND_URL: http://localhost:8080/api
      USE_OLLAMA_DOCKER: "true"
      OLLAMA_BASE_URL: "http://127.0.0.1:11434"
      ENABLE_OLLAMA_API: "true"
      OLLAMA_HOST: "0.0.0.0"
    volumes:
      - ollama:/root/.ollama
      - open-webui:/app/backend/data
    restart: always
    networks:
      - internal-prod

  tailscale-sidecar:
    image: tailscale/tailscale:latest
    container_name: tailscale-sidecar
    hostname: jarvis
    privileged: true
    profiles:
      - prod
      - all
    environment:
      TS_AUTHKEY: ${TS_AUTHKEY}
      TS_HOSTNAME: jarvis
      TS_ACCEPT_DNS: true
      TS_STATE_DIR: /var/lib/tailscale
    volumes:
      - tailscale-sidecar-state:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    restart: always
    network_mode: host
    env_file:
      - production/.env

  nginx-prod:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: always
    profiles:
      - prod
      - all
    volumes:
      - ./production/nginx.conf:/etc/nginx/nginx.conf:ro,z
    depends_on:
      - open-webui2
    networks:
      - internal-prod
    ports:
      - "0.0.0.0:8080:80"

  # ============================================
  # BETA STACK
  # ============================================
  # Beta - ROLLING tag to always get latest features for testing
  open-webui-beta:
    image: ghcr.io/open-webui/open-webui:main-ollama
    container_name: open-webui-beta
    ports:
      - "11435:11434"

    profiles:
      - beta
      - all
    environment:
      BACKEND_URL: http://localhost:8081/api
      USE_OLLAMA_DOCKER: "true"
      OLLAMA_BASE_URL: "http://127.0.0.1:11434"
      ENABLE_OLLAMA_API: "true"
      OLLAMA_HOST: "0.0.0.0"
    volumes:
      - ollama-beta:/root/.ollama
      - open-webui-beta:/app/backend/data
      - ./beta/assets/favicon-beta.svg:/app/backend/static/favicon.png:ro,z
      - ./beta/assets/logo-beta.svg:/app/backend/static/logo.svg:ro,z
    restart: always
    networks:
      - internal-beta

  tailscale-sidecar-beta:
    image: tailscale/tailscale:latest
    container_name: tailscale-sidecar-beta
    hostname: jarvis-beta
    network_mode: host
    privileged: true
    profiles:
      - beta
      - all
    environment:
      TS_AUTHKEY: ${TS_AUTHKEY_BETA}
      TS_HOSTNAME: jarvis-beta
      TS_ACCEPT_DNS: true
      TS_STATE_DIR: /var/lib/tailscale
    volumes:
      - tailscale-sidecar-beta-state:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - NET_ADMIN
      - SYS_MODULE
    restart: always
    env_file:
      - beta/.env

  nginx-beta:
    image: nginx:alpine
    container_name: nginx-beta
    restart: always
    profiles:
      - beta
      - all
    volumes:
      - ./beta/nginx.conf:/etc/nginx/nginx.conf:ro,z
    depends_on:
      - open-webui-beta
    networks:
      - internal-beta
    ports:
      - "0.0.0.0:8081:80"

networks:
  internal-prod:
    driver: bridge
  internal-beta:
    driver: bridge

volumes:
  # Production volumes
  ollama:
    driver: local
  open-webui:
    driver: local
  tailscale-sidecar-state:
    driver: local

  # Beta volumes
  ollama-beta:
    driver: local
  open-webui-beta:
    driver: local
  tailscale-sidecar-beta-state:
    driver: local
